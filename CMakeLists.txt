project(RanSaCSieve)
cmake_minimum_required(VERSION 2.8)
aux_source_directory(. SRC_LIST)
include_directories(${CMAKE_SOURCE_DIR})
message("Includes ${CMAKE_SOURCE_DIR}")
message("Compiling ${SRC_LIST}")
add_executable(${PROJECT_NAME} ${SRC_LIST})
